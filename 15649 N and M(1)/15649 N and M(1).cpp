// 15649 N and M(1).cpp : 이 파일에는 'main' 함수가 포함됩니다. 거기서 프로그램 실행이 시작되고 종료됩니다.
//

#include "pch.h"
#include <iostream>
#include <vector>

using namespace std;

/*
	if we visit one, and we will never visit.
	It's the backtracking method.
*/

vector<int> Permutation;

void loadPermutation(const int& M, const int& N)
{
	/*if (Permutation.size() == N)
	{

	}*/
}

int main()
{
	int M, N;
	cin >> M >> N;
}
